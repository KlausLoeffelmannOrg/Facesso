# Copilot Instructions

## Project Guidelines
- Avoid kicking off multiple bulk operations in Visual Studio at the same time. Process each project conversion, build, and commit one at a time sequentially — never run multiple convert_project_to_sdk_style, build, or other heavy VS operations in parallel.